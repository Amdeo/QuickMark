# QuickMark 领域词表

本表只收录领域概念与不变量，不含实现细节。

## 面板 (Panel)

快捷键呼出的浮层界面，是键盘操作的唯一入口。同一时刻只有一个面板；搜索面板
（Cmd/Ctrl+Shift+K）与标签切换面板（Cmd/Ctrl+Shift+O）互斥，弹出即接管输入。

## 输入就绪 (Keyboard-ready)

"面板已打开"不等于"可以键盘操作"。输入就绪 = 面板可见、焦点已在搜索输入框、
快捷键语义成立（上下键移动选择、回车打开、空格展开最近搜索）。**不变量：面板
出现时必须输入就绪**——否则按键会穿过面板落到页面上，面板看起来开了却不可用。

## 骨架占位 (Skeleton)

冷启动（搜索大包尚未解析）时先于真实面板出现的占位层：与真实面板同款入场动画，
真实面板就绪后交叉淡出。热路径（大包已缓存）不出现骨架，直接显示真实面板——
占位只用于覆盖真实面板无法立即渲染的等待期。