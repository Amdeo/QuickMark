# QuickMark 使用指南

## 安装

1. 打开 Chrome，访问 `chrome://extensions/`
2. 开启右上角「开发者模式」
3. 点击「加载已解压的扩展程序」
4. 选择 `QuickMark/dist` 文件夹

## 打开搜索

**快捷键**：`Cmd/Ctrl + Shift + K`

- 在任意网页按下快捷键，弹出搜索覆盖层
- 输入关键词，模糊搜索书签和浏览器历史
- **拼音搜索**：中文书签可直接输入拼音命中（如 `zhihu` → 知乎、`wendang` → React 文档）
- 搜索范围同时覆盖书签与历史记录，每条结果独立显示；不按域名折叠，也不自动插入首页
- **地址栏直跳**：输入完整网址或域名（如 `github.com`、`localhost:3000`）回车直接跳转，无需本地结果
- 若无本地结果，可一键跳转 Google 搜索

### 导航与操作

| 按键 | 操作 |
| --- | --- |
| `↑/↓` | 选择结果 |
| `Enter` | 在当前标签页打开 |
| `Cmd/Ctrl + Enter` | 在新标签页打开 |
| `Cmd/Ctrl + 1–9` | 打开第 N 个固定图标，之后顺延到第 N 条结果；固定区可见时图标先占号，输入关键词后数字键直接对应结果 |
| `Cmd/Ctrl + C` | 复制选中结果的链接 |
| `Esc` | 第一次清空搜索词，第二次关闭 |
| `Space` | 搜索框为空时展开最近搜索下拉（↑↓ 选择，回车回填）；有输入时空格照常输入 |
| `Alt + P` | 固定 / 取消固定选中的网站 |
| `Alt + ←/→` | 切换来源筛选，普通左右键移动搜索光标 |

### 筛选与排序

- **来源筛选**：全部 / 书签 / 历史（`Alt + ←/→` 键循环切换）
- **筛选下拉**：筛选栏右侧的下拉同时管时间范围与排序，菜单分「时间范围」「排序」两组；按钮上直接显示已生效的非默认选项（如「今天 · 使用频率」），都是默认值时显示「筛选」。
- **时间范围**：全部时间 / 今天 / 本周 / 本月
- **排序模式**：智能排序 / 最近访问 / 使用频率 / 标题 A-Z / 创建时间 / 相关度优先
- **自动记忆**：所选排序模式保存到本地，下次打开面板自动恢复，其他标签页同步更新。
- **智能排序**：最近访问为主，兼顾访问频率；长期未用的记录降权，不给新建但未访问的书签额外加分。“最近访问”只看真实访问时间，“使用频率”只按访问次数。有关键词时匹配程度优先，所选模式用于相近结果；“相关度优先”保持纯搜索相关性。

### 固定常用网站

- 点击结果右侧的图钉，或选中后按 `Alt + P`，将完整网址固定到筛选栏里、来源筛选右侧的一行图标里。
- 最多 8 个，只显示图标，行首有一个钉子标记提示用途，按添加顺序固定，不会被智能排序替换；没有固定任何网站时这一行不占位，面板变窄时图标整组换到下一行。
- 图标左上角的数字就是它的快捷键：`Cmd/Ctrl + 1–8` 直接打开对应图标，顺序即从左到右；按住 `Cmd/Ctrl` 点击则在新标签页打开。
- **拖拽排序**：把一个图标拖到另一个图标上即可交换位置，顺序会自动保存，编号和快捷键随之更新。
- 结果行角标始终等于它真实的数字键：固定了 N 个时，第一条结果是 `Cmd/Ctrl + N+1`；输入关键词后固定区隐藏，数字键重新从第一条结果开始。
- 鼠标悬停（或键盘聚焦）图标可看到网站名称、快捷键和完整网址。
- 悬停或聚焦后点击图标右上角的小按钮即可取消固定；满 8 个时先取消一个再添加，未满时可继续固定。
- 固定区在搜索词为空时显示；保存于本地，即使原书签或历史被删除仍可直达。

### 其他功能

- **搜索历史**：最近 5 条搜索词，在搜索框为空时按空格打开下拉，方向键选择、回车回填；下拉底部的「清空最近搜索」可一次清空
- **主题**：右下角按钮在 浅色 / 深色 / 跟随系统 间循环
- **复制链接**：结果行悬停或选中后点击复制按钮，成功有 ✓ 反馈

## 快捷键自定义

打开 `chrome://extensions/shortcuts`，找到 QuickMark 即可修改快捷键。

## 数据存储

搜索数据来自 Chrome 本地的书签和历史 API，缓存于 `chrome.storage.local`，不上传任何数据。书签/历史变更后会自动后台刷新缓存。排序模式与固定网站分别保存在 `quickmark-sort-mode` 和 `quickmark-pinned-sites`，独立于浏览历史缓存。
