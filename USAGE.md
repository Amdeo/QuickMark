# QuickMark 使用指南

## 安装

1. 打开 Chrome，访问 `chrome://extensions/`
2. 开启右上角「开发者模式」
3. 点击「加载已解压的扩展程序」
4. 选择 `QuickMark/dist` 文件夹

## 打开搜索

**快捷键**：`Cmd/Ctrl + Shift + K`

- 在任意网页按下快捷键，弹出搜索覆盖层
- 输入关键词，模糊搜索书签和浏览器历史
- **拼音搜索**：中文书签可直接输入拼音命中（如 `zhihu` → 知乎、`wendang` → React 文档）
- 搜索范围同时覆盖书签与历史记录，结果按域名分组折叠
- **地址栏直跳**：输入完整网址或域名（如 `github.com`、`localhost:3000`）回车直接跳转，无需本地结果
- 若无本地结果，可一键跳转 Google 搜索

### 导航与操作

| 按键 | 操作 |
| --- | --- |
| `↑/↓` | 选择结果 |
| `Enter` | 在当前标签页打开 |
| `Cmd/Ctrl + Enter` | 在新标签页打开 |
| `Cmd/Ctrl + 1–9` | 直达第 N 个结果 |
| `Cmd/Ctrl + C` | 复制选中结果的链接 |
| `Esc` | 第一次清空搜索词，第二次关闭 |
| 点击分组标题 | 展开/折叠同一域名的结果 |

### 筛选与排序

- **来源筛选**：全部 / 书签 / 历史（`←/→` 键循环切换）
- **时间范围**：全部时间 / 今天 / 本周 / 本月
- **排序模式**：智能排序 / 最近访问 / 使用频率 / 标题 A-Z / 创建时间 / 相关度优先

### 其他功能

- **搜索历史**：最近 5 条搜索词，可一键回填或清空
- **主题**：右下角按钮在 浅色 / 深色 / 跟随系统 间循环
- **复制链接**：结果行悬停或选中后点击复制按钮，成功有 ✓ 反馈

## 快捷键自定义

打开 `chrome://extensions/shortcuts`，找到 QuickMark 即可修改快捷键。

## 数据存储

搜索数据来自 Chrome 本地的书签和历史 API，缓存于 `chrome.storage.local`，不上传任何数据。书签/历史变更后会自动后台刷新缓存。
